//= require natural_sorting

$(function() {
	$('#users_table').dataTable( {
		"order": [[ 3, "asc" ], [ 2, "asc"]],
		responsive: true,
		"pageLength": 25
	});
	$('#events_table').dataTable( {
		"order": [[ 1, "desc" ]],
		responsive: true,
		"pageLength": 25
	});
});