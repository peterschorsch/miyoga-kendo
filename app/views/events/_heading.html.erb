<% if current_user.is_not_admin? %>
	<div class="row">
		<div class="col-md-12"><h1 class="no-margin"><%= event.title %></h1></div>
	</div>
<% else %>
	<div class="row vertical-align-middle">
		<div class="col-md-8"><h1 class=""><%= event.title %></h1></div>
		<div class="col-md-2"><%= render "templates/buttons/crud/edit-button-tag", target: "#eventEditModal-#{index}" %></div>
		<div class="col-md-2"><%= render "templates/buttons/crud/remove-button-tag", target: "#youSureModal-#{index}" %></div>
	</div>
<% end %>
<hr />