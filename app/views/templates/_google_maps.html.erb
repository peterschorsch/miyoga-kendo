<%= embed_google_maps(address: "#{address.location_name} #{address.address_line_1} #{address.address_line_2} #{address.city} #{address.state.abbreviation}, #{address.zip_code}", height: height, width: width).html_safe %>